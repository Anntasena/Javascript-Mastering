// expresion value

2 + 2;
1992;
true && false && !false;

// statement value
// statement value seperti potongan kode yang lebih besar yang diesekusi dan tidak menghasilkan nilai pada dirinya sendiri
// if else adalah statement value
if (23 > 10) {
    const str = `23 is bigger`; // biasanya yang di akhiri dengan titik koma itu statement
}

const wow = `wow`

console.log(`I'm ${2024 - 1998} years old ${wow}`);